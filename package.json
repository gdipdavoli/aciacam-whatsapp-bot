{
  "name": "aciacam-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "release": "standard-version",
    "test": "echo \"(opcional) agrega tus tests\" && exit 0",

    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js",

    "deploy:stg": "gcloud run deploy aciacam-whatsapp-bot-stg --region us-central1 --source . --allow-unauthenticated --env-vars-file=envvars.yaml --update-secrets OPENAI_API_KEY=OPENAI_API_KEY:latest --memory=1Gi --cpu=1 --concurrency=1 --timeout=600",
    "deploy:prod": "gcloud run deploy aciacam-whatsapp-bot --region us-central1 --source . --allow-unauthenticated --env-vars-file=envvars.yaml --update-secrets OPENAI_API_KEY=OPENAI_API_KEY:latest --memory=1Gi --cpu=1 --concurrency=1 --timeout=600",

    "logs:stg": "gcloud beta run services logs tail aciacam-whatsapp-bot-stg --region us-central1",
    "logs:prod": "gcloud beta run services logs tail aciacam-whatsapp-bot --region us-central1",

    "state:stg": "open https://aciacam-whatsapp-bot-stg-466998795303.us-central1.run.app/state || true",
    "state:prod": "open https://aciacam-whatsapp-bot-466998795303.us-central1.run.app/state || true",

    "test:mongo": "node test-mongo.js"
}

  "dependencies": {
    "@ffprobe-installer/ffprobe": "^2.1.2",
    "axios": "^1.12.2",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "ffmpeg-static": "^5.2.0",
    "fluent-ffmpeg": "^2.1.3",
    "googleapis": "^161.0.0",
    "mongodb": "^6.20.0",
    "mongoose": "^8.19.1",
    "openai": "^6.1.0",
    "qrcode": "^1.5.4",
    "whatsapp-web.js": "^1.34.1",
    "wwebjs-mongo": "^1.1.0"
  }
}
